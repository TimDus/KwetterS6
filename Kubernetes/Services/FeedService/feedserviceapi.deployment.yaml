apiVersion: v1
kind: Service
metadata:
  name: feedapi
  namespace: kwetter
spec:
  selector:
    app: feedapi
  ports:
    - protocol: TCP
      port: 8104
      targetPort: 8104
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: feedapi
  namespace: kwetter
spec:
  replicas: 1
  minReadySeconds: 10
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: feedapi
      version: latest
  template:
    metadata:
      labels:
        app: feedapi
        version: latest
    spec:
      containers:
      - name: feedapi
        image: timdus/feedapi:latest
        ports:
        - containerPort: 8104
        imagePullPolicy: Always
        resources:
          limits:   
            memory: 512Mi
            cpu: "1"